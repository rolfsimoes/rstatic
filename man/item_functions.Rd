% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item-funs.R
\name{item_functions}
\alias{item_functions}
\alias{new_properties}
\alias{new_asset}
\alias{new_item}
\alias{add_items}
\title{Create STAC items, properties, and assets}
\usage{
new_properties(
  description = NULL,
  datetime = NULL,
  start_datetime = NULL,
  end_datetime = NULL,
  start_date = NULL,
  end_date = NULL,
  ...
)

new_asset(href, title = NULL, roles = list("data"), ...)

new_item(
  id,
  bbox,
  geometry = NULL,
  properties = new_properties(),
  assets = list(),
  collection = NULL,
  stac_version = "1.0.0",
  ...
)

add_items(collection, items)
}
\arguments{
\item{description}{A \code{character} description.}

\item{datetime}{A \code{character} RFC 3339 datetime, or \code{NULL}. Recorded as
\code{null} when omitted alongside a \code{start_datetime}/\code{end_datetime} range.}

\item{start_datetime}{A \code{character} RFC 3339 start datetime, or \code{NULL}.}

\item{end_datetime}{A \code{character} RFC 3339 end datetime, or \code{NULL}.}

\item{start_date}{A \code{character} start date, or \code{NULL}.}

\item{end_date}{A \code{character} end date, or \code{NULL}.}

\item{...}{Additional named fields. See details for each
function.}

\item{href}{A \code{character} asset target (path or URL).}

\item{title}{A \code{character} title.}

\item{roles}{A \code{list} of asset roles. Defaults to \code{list("data")}.}

\item{id}{A \code{character} identifier for the item.}

\item{bbox}{A numeric vector \code{c(xmin, ymin, xmax, ymax)}.}

\item{geometry}{A GeoJSON geometry \code{list}. If \code{NULL}, it is derived
from \code{bbox} via \code{\link[=as_geometry]{as_geometry()}}.}

\item{properties}{A \code{list} of item properties, e.g. from
\code{new_properties()}.}

\item{assets}{A named \code{list} of assets, e.g. from \code{new_asset()}.}

\item{collection}{For \code{add_items()}, the \code{doc_collection} to link items
into. For \code{new_item()}, an optional \code{doc_collection} or \code{character}
collection id recorded as the item's top-level \code{collection} field. STAC
requires this field whenever the item carries a \code{collection} link (as items
built here always do); when omitted, \code{\link[=stac_save]{stac_save()}} stamps it from the
\code{collection} it is given.}

\item{stac_version}{A \code{character} STAC version. Defaults to \code{"1.0.0"}.}

\item{items}{A single \code{doc_item}, or a \code{list} of \code{doc_item}
objects, to link into the collection.}
}
\value{
\itemize{
\item \code{new_properties()}: a \code{list} of properties.
\item \code{new_asset()}: a \code{doc_asset} object describing an asset.
\item \code{new_item()}: a \code{doc_item} object.
\item \code{add_items()}: the updated \code{doc_collection}.
}
}
\description{
Pure builders for the building blocks of a STAC \code{Item}. None touch disk; use
\code{\link[=stac_save]{stac_save()}} to persist.

\itemize{
\item \code{new_properties()}: assembles an item \code{properties} list.
\item \code{new_asset()}: creates a single STAC \code{Asset}.
\item \code{new_item()}: creates an in-memory \code{Item} (a GeoJSON \code{Feature}).
\item \code{add_items()}: links one or more items into a collection, updating
the collection links and spatio-temporal extent, and returns the
collection.
}

STAC requires every item to carry a \code{datetime}. When you describe a time
range instead of a single instant, supply \code{start_datetime} and \code{end_datetime}
and omit \code{datetime}; \code{new_properties()} then records \code{datetime} as \code{null}, as
the specification mandates. \code{add_items()} derives the collection's temporal
extent from the range (or from \code{datetime} when only that is given).
}
\examples{
props <- new_properties(datetime = "2020-01-01T00:00:00Z")
asset <- new_asset("data.tif", title = "Data")
item <- new_item(
  id = "item-1",
  bbox = c(-50, -10, -49, -9),
  properties = props,
  assets = list(data = asset)
)
item$type

col <- new_collection("col", "Collection", "Example")
col <- add_items(col, item)
col$extent$spatial$bbox[[1]]
}
