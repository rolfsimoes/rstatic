% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link-funs.R
\name{list_links}
\alias{list_links}
\title{List and filter the links of a STAC document}
\usage{
list_links(x, ...)
}
\arguments{
\item{x}{A STAC document (\code{doc_catalog}, \code{doc_collection}, or \code{doc_item}),
or a bare \code{list} of links.}

\item{...}{Optional filter expressions evaluated against each link, e.g.
\code{rel == "child"}. Combined with logical AND.}
}
\value{
A \code{list} of links (each element as stored on the document). The list
is empty when nothing matches.
}
\description{
Returns the links of a STAC document as a plain \code{list}, optionally keeping
only those that satisfy one or more filter expressions. Each expression in
\code{...} is evaluated against a single link, with the link's fields (\code{rel},
\code{href}, \code{type}, \code{title}) available as names, and the expressions are combined
with logical AND. With no expressions, every link is returned.

This mirrors the ergonomics of \code{rstac::links()}: instead of writing
\code{Filter(function(l) l$rel == "child", doc$links)}, you write
\code{list_links(doc, rel == "child")}.

A link whose fields make an expression error (for example a link that has no
\code{title}) is treated as not matching, rather than raising an error. Variables
from the calling scope may be used in the expressions.
}
\examples{
catalog <- new_catalog("cat", "Catalog", "Example")
catalog <- add_collection(catalog, new_collection("a", "A", "First"))
catalog <- add_collection(catalog, new_collection("b", "B", "Second"))

# All links
list_links(catalog)

# Only the child links
list_links(catalog, rel == "child")

# Combine predicates (logical AND)
list_links(catalog, rel == "child", type == "application/json")

}
