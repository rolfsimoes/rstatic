% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qml-funs.R
\name{qml_to_style}
\alias{qml_to_style}
\title{Convert a QGIS QML raster style to a thumbnail style}
\usage{
qml_to_style(qml_path)
}
\arguments{
\item{qml_path}{A \code{character} path or URL to a QGIS \code{.qml} file.}
}
\value{
A normalized \code{rstatic_style} object, as produced by \code{\link[=stac_style]{stac_style()}}.
}
\description{
\code{qml_to_style()} reads a supported QGIS \code{.qml} raster style and converts it
to the same normalized style object produced by \code{\link[=stac_style]{stac_style()}}.

This function supports a limited subset of QGIS raster renderers: paletted,
single-band gray, and single-band pseudocolor. Other renderers are rejected
with a clear error.

The function is a converter, not a general QML parser. QGIS-specific details
are normalized to the \code{rstatic} style model. It requires the optional
\pkg{xml2} package; if \pkg{xml2} is not installed, build the style manually
with \code{\link[=stac_style]{stac_style()}}.
}
\examples{
if (requireNamespace("xml2", quietly = TRUE)) {
  qml <- system.file("extdata/example.qml", package = "rstatic")
  if (nzchar(qml)) {
    qml_to_style(qml)
  }
}

}
