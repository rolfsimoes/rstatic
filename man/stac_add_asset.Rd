% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item-funs.R
\name{stac_add_asset}
\alias{stac_add_asset}
\title{Add an asset to a STAC document}
\usage{
stac_add_asset(doc, key, asset)
}
\arguments{
\item{doc}{A STAC document (\code{doc_item} or \code{doc_collection}).}

\item{key}{A \code{character} name for the asset in the document's \code{assets}
map.}

\item{asset}{A \code{list} describing the asset, as from \code{\link[=new_asset]{new_asset()}}.}
}
\value{
The updated STAC document, with \code{asset} stored under
\code{doc$assets[[key]]} and the appropriate class preserved.
}
\description{
Pure builder that attaches an asset to a STAC \code{Item} or \code{Collection}.
Assets set at creation time via \code{\link[=new_item]{new_item()}} can be complemented or
overwritten later with this function. No disk I/O is performed.
}
\examples{
item <- new_item("i", bbox = c(0, 0, 1, 1))
item <- stac_add_asset(item, "data", new_asset("data.tif"))

}
