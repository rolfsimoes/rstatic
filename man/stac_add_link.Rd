% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link-funs.R
\name{stac_add_link}
\alias{stac_add_link}
\title{Add a link to a STAC document}
\usage{
stac_add_link(doc, rel, href, type = "application/json", title = NULL)
}
\arguments{
\item{doc}{A STAC document (\code{doc_catalog}, \code{doc_collection}, or
\code{doc_item}).}

\item{rel}{A \code{character} link relation (\code{self}, \code{root}, \code{parent}, \code{child},
\code{item}, \code{collection}, etc.).}

\item{href}{A \code{character} link target (path or URL). Relative paths are
recommended for static catalogs.}

\item{type}{A \code{character} media type. Defaults to \code{"application/json"}.}

\item{title}{An optional \code{character} human-readable title for the link.}
}
\value{
The updated STAC document, with the new link appended and the
appropriate \verb{doc_*} class preserved.
}
\description{
Pure builder that appends a link to a STAC document's \code{links} list.
The document is returned unchanged if a link with the same \code{href}
already exists. No disk I/O is performed.
}
\examples{
cat <- new_catalog("cat", "Catalog", "Example")
cat <- stac_add_link(cat, "child", "collections/col/collection.json",
                     title = "My Collection")

item <- new_item("i", bbox = c(0, 0, 1, 1))
item <- stac_add_link(item, "derived_from", "source.json")

}
