% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-funs.R
\name{stac_read}
\alias{stac_read}
\title{Read a STAC document from disk}
\usage{
stac_read(path, default = NULL)
}
\arguments{
\item{path}{A \code{character} path to a STAC JSON file.}

\item{default}{An optional in-memory \verb{doc_*} object returned when \code{path} does
not exist. If \code{NULL} (the default), a missing file is an error.}
}
\value{
A \code{doc_catalog}, \code{doc_collection}, or \code{doc_item} object.
}
\description{
Reads a STAC \code{Catalog}, \code{Collection}, or \code{Item} JSON file from disk and
returns it as an in-memory \verb{doc_*} object. This is the only reader in the
package; together with \code{\link[=stac_save]{stac_save()}} it forms the effectful boundary around
the pure constructors (\verb{new_*}) and builders (\verb{add_*}).

When the file does not exist, \code{stac_read()} returns \code{default} if one is
supplied (e.g. a freshly built \code{new_catalog()}), otherwise it errors. This
supports the load-or-create pattern without ever writing to disk:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{catalog <- stac_read(
  file.path(root, "stac", "catalog.json"),
  default = new_catalog("my-catalog", "My Catalog", "...")
)
}\if{html}{\out{</div>}}
}
\examples{
dir <- tempfile("stac-")
cat <- new_catalog("c", "Catalog", "An example catalog")
stac_save(catalog = cat, root_dir = dir)

# Read it back
path <- file.path(dir, "stac", "catalog.json")
stac_read(path)$id

# Missing file with a default returns the default, without writing
stac_read(file.path(dir, "missing.json"),
          default = new_catalog("fallback", "Fallback", "..."))$id

}
