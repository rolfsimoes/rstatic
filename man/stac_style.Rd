% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style-funs.R
\name{stac_style}
\alias{stac_style}
\title{Define a raster style for thumbnail rendering}
\usage{
stac_style(
  bands = NULL,
  min = NULL,
  max = NULL,
  pmin = NULL,
  pmax = NULL,
  palette = NULL,
  values = NULL,
  colors = NULL,
  labels = NULL,
  nodata = NULL,
  opacity = NULL,
  gamma = NULL
)
}
\arguments{
\item{bands}{Optional band name or vector of three band names. Three bands
indicate RGB rendering.}

\item{min}{Minimum value used for continuous stretching. Must have length
one or three. Length three is only allowed when \code{bands} has length three.}

\item{max}{Maximum value used for continuous stretching. Must have length
one or three. Length three is only allowed when \code{bands} has length three.}

\item{pmin}{Lower percentile used to derive the stretch minimum from the
image. Must have length one or three. Length three is only allowed when
\code{bands} has length three.}

\item{pmax}{Upper percentile used to derive the stretch maximum from the
image. Must have length one or three. Length three is only allowed when
\code{bands} has length three.}

\item{palette}{Color ramp for continuous single-band rendering. It can be a
known palette name or a vector of colors.}

\item{values}{Raster values for categorical rendering.}

\item{colors}{Colors associated with \code{values}.}

\item{labels}{Optional labels associated with \code{values}. Defaults to
\code{as.character(values)}.}

\item{nodata}{Optional value to render as transparent. It is applied before
all other rendering rules.}

\item{opacity}{Optional global opacity between \code{0} and \code{1} for valid pixels.}

\item{gamma}{Optional gamma correction for continuous rendering. It must
not be used with categorical styles.}
}
\value{
A normalized \code{rstatic_style} object. The object also carries a mode-specific
subclass: \code{rstatic_style_categorical}, \code{rstatic_style_continuous}, or
\code{rstatic_style_rgb}.
}
\description{
\code{stac_style()} creates a normalized style object used by \code{\link[=new_thumbnail]{new_thumbnail()}}
to render raster previews. The style describes how raster values are mapped
to thumbnail pixels. It does not render the image.

Continuous styles use \code{min}/\code{max} or \code{pmin}/\code{pmax}, optionally with a color
ramp supplied by \code{palette}. RGB rendering is represented by passing three
band names to \code{bands}.

Categorical styles use explicit \code{values} and \code{colors} mappings. Values do
not need to be sequential. Optional \code{labels} describe the categorical values
and may be used to derive legends.

\code{nodata} is applied before any rendering rule and is rendered as transparent.
}
\examples{
# Continuous grayscale stretch
stac_style(min = 0, max = 0.5, palette = c("black", "white"))

# Single-band percentile stretch
stac_style(bands = "B04", pmin = 0.02, pmax = 0.98, palette = "viridis")

# RGB composite from three bands
stac_style(bands = c("B04", "B03", "B02"), pmin = 0.02, pmax = 0.98)

# Categorical land-cover mapping
stac_style(
  values = c(1, 2, 3),
  colors = c("#c14d00", "#367906", "#7cc900"),
  labels = c("Crop", "Forest", "Grassland"),
  nodata = 0
)

}
