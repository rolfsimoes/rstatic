% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style-funs.R
\name{style_functions}
\alias{style_functions}
\alias{stac_style}
\alias{qml_to_style}
\title{STAC style objects for thumbnails}
\usage{
stac_style(
  min = NULL,
  max = NULL,
  pmin = NULL,
  pmax = NULL,
  palette = NULL,
  legend = NULL
)

qml_to_style(qml_path)
}
\arguments{
\item{min}{Minimum value for stretching or color mapping.}

\item{max}{Maximum value for stretching or color mapping.}

\item{pmin}{Percentile minimum for stretching (e.g. \code{0.02}).}

\item{pmax}{Percentile maximum for stretching (e.g. \code{0.98}).}

\item{palette}{Color palette name or a vector of colors.}

\item{legend}{A \code{data.frame} with \code{value} and \code{color} columns for
categorical data.}

\item{qml_path}{A \code{character} path or URL to a QGIS \code{.qml} style file.}
}
\value{
A \code{list} with style parameters: a \code{categorical}, \code{continuous}, or
\code{simple} style depending on the inputs.
}
\description{
Helpers to describe how a raster should be rendered into a thumbnail.

\itemize{
\item \code{stac_style()}: builds a style object from explicit parameters.
This function has no external dependencies.
\item \code{qml_to_style()}: parses a QGIS Layer Style file (\code{.qml}) into a
style object. This function requires the optional \pkg{xml2} package.
}

If \pkg{xml2} is not installed, build the style manually with \code{stac_style()}
instead of reading a \code{.qml} file.
}
\examples{
# Build a style directly (no optional dependency required)
stac_style(min = 0, max = 255, palette = c("black", "white"))

# Parse a QML file (requires xml2)
if (requireNamespace("xml2", quietly = TRUE)) {
  qml <- system.file("extdata/example.qml", package = "rstatic")
  if (nzchar(qml)) {
    qml_to_style(qml)
  }
}
}
